feat: admin refinements, dual-admin access, store and approvals redesign

Security / Admin Access
- Add anderson.correavaz@gmail.com to FALLBACK_ADMINS in all 7 Netlify functions
- Add FALLBACK_ADMINS pattern to get-passcode.ts and update-passcode.ts (previously had no fallback)
- Both admin emails now have identical backend authorization

Admin Dashboard - Approvals Tab (ManagementTab.tsx)
- Passcode Gate Leads table now shows Name + Email + Status + Captured columns
- Add StatusPill, SectionHeader, EmptyState shared UI components
- Redesign MemberAccessCard and ApplicationCard with platform-style dark cards
- Replace inline status spans with color-coded StatusPill component

Admin Dashboard - Store Tab (StoreTab.tsx + AdminStoreItemListItem.tsx)
- Remove duplicate outer search bar above Store tab (AdminDashboard.tsx)
- Redesign store item cards: thumbnail with hover zoom, category pills, gold TMKC badge
- Replace dropdown category filter with pill buttons (All / Merchandise / NFT / Perk)
- Replace orange CTA with platform gradient (purple to violet to cyan)

Event Timeline (WingmanEventFeed.tsx)
- Rebuild layout to match Venues page: full-width cards, tall cover photos, gradient overlays

Data Cleanup (App.tsx, mockData.ts)
- Remove all fake/demo seed users and example.com records
- Implement wm_purge_v2 localStorage purge on first load

Build: npx tsc --noEmit passes with 0 errors
