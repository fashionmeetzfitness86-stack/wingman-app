fix: audit cleanup - passcode tooltip, push campaigns persistence

AccessControlTab.tsx
- Fix passcode rules tooltip: was "6-20 chars, letters/numbers only"
- Now correctly says "6-30 characters (case-sensitive), any chars no spaces"

App.tsx
- Persist pushCampaigns to localStorage on create, toggle, and delete
- Campaigns now survive page refresh (previously wiped on every reload)
- Fix TypeScript type error: cast toggled status as 'active' | 'inactive' literal
